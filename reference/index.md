# Reference

API reference docs are coming soon.

In the meantime, the [guides](/guide/) walk through the full `init()` / `store()` / `retrieveByRefId()`
call pattern with working code for every adapter combination.
